pipeline {
    agent any
    stages {
        stage('echo') {
          steps {
            sh 'echo "hello"'
          }
        }
        // stage('build') {
        //   steps {
        //     sh 'npm'
        //   }
        // }
        // stage('test') {
        //   steps {
        //     sh 'npm run test'
        //   }
        // }
    }
}
