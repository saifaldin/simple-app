pipeline {
    agent any
    stages {
        stage('test') {
          steps {
            sh 'ls -lh'
          }
        }
    // stage('build') {
    //   steps {
    //     sh 'npm'
    //   }
    // }
    // stage('test') {
    //   steps {
    //     sh 'npm run test'
    //   }
    // }
    }
}
